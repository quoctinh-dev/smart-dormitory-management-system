Chào Agent, tôi có một ý tưởng mới về tính năng sẽ làm trong tương lai cho hệ thống (Future Plan).

Hãy lắng nghe ý tưởng của tôi dưới đây, sau đó phân tích và đóng gói nó thành một file Markdown lưu vào thư mục `docs/roadmap/features/`.
Tên file phải tuân theo quy tắc: `[STT]_[TÊN_TÍNH_NĂNG_VIẾT_HOA_KHÔNG_DẤU].md` (Ví dụ: `02_PAYMENT_GATEWAY_V2.md`).

Cấu trúc file phải tuân thủ chuẩn sau:
1. **Tầm nhìn và Mục tiêu:** Tính năng này giải quyết nỗi đau gì của user?
2. **Luồng nghiệp vụ mới:** Phác thảo luồng đi (ví dụ: User bấm nút -> API Backend gọi -> DB lưu trữ).
3. **Lộ trình triển khai (Roadmap):** Phân chia công việc rõ ràng cho từng module (Backend, Frontend, IoT, AI, DB Schema).
4. **Prompt Kích hoạt:** Viết sẵn một đoạn văn bản (Prompt) ở cuối file để sau này tôi chỉ việc copy paste gửi cho AI khi tôi thực sự muốn bắt tay vào code tính năng này.

Ý TƯỞNG CỦA TÔI LÀ:
[Bạn hãy điền ý tưởng của bạn vào đây]


Chào Agent, hãy chuyển hóa ý tưởng về tính năng sẽ làm trong tương lai cho hệ thống (Future Plan) của IOT mà ta đã thảo luận
sau đó phân tích và đóng gói nó thành một file Markdown lưu vào thư mục `docs/roadmap/features/`.
Tên file phải tuân theo quy tắc: `[STT]_[TÊN_TÍNH_NĂNG_VIẾT_HOA_KHÔNG_DẤU].md` (Ví dụ: `02_PAYMENT_GATEWAY_V2.md`).

Cấu trúc file phải tuân thủ chuẩn sau:
1. **Tầm nhìn và Mục tiêu:** Tính năng này giải quyết nỗi đau gì của user?
2. **Luồng nghiệp vụ mới:** Phác thảo luồng đi (ví dụ: User bấm nút -> API Backend gọi -> DB lưu trữ).
3. **Lộ trình triển khai (Roadmap):** Phân chia công việc rõ ràng cho từng module (Backend, Frontend, IoT, AI, DB Schema).
4. **Prompt Kích hoạt:** Viết sẵn một đoạn văn bản (Prompt) ở cuối file để sau này tôi chỉ việc copy paste gửi cho AI khi tôi thực sự muốn bắt tay vào code tính năng này.
