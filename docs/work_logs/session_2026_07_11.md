# Lịch Sử Phiên Làm Việc (Session Log)
**Ngày:** 2026-07-11
**Mô-đun trọng tâm:** Smart Access (IoT), Room Management (PIN Integration), System & Database Refactoring.

## 1. Các Công Việc Đã Hoàn Thành (Completed Tasks)
* **Nâng cấp Face AI (512-Dimension):** Cập nhật hệ thống nhận diện khuôn mặt và database để hỗ trợ vector nhúng (embeddings) 512 chiều (512-dimension), tăng cường độ chính xác cho mô hình AI.
* **Chuẩn hóa Hardware IoT (ESP32):** Cập nhật lại toàn bộ tài liệu sơ đồ chân (Pinout) từ mạch ESP32 38-pin sang chuẩn **ESP32 DevKit V1 30-pin Type-C**. Xác nhận mã nguồn C++ (`room_door`) hoàn toàn khớp với Backend API.
* **Tái cấu trúc Kiến trúc Room PIN (Backend):**
  * **Chuyển giao quyền sở hữu PIN:** Chuyển mã PIN từ thực thể `Student` sang `Room` (`V44__add_room_pin_code_to_rooms.sql`). Đảm bảo khi sinh viên đổi phòng, hệ thống tự động đồng bộ quyền truy cập (chỉ xét duyệt `OCCUPIED`).
  * **API & Logic:** Xây dựng `RoomPinController` và `RoomPinService`. Hỗ trợ tự động sinh PIN ngẫu nhiên 6 số (SecureRandom) cho tính năng "Tạo mới PIN (Reset)" và "Sinh hàng loạt (Bulk Generate)".
  * **Hệ thống Thông báo (Event-Driven):** Tích hợp `RoomPinChangedEvent` vào `NotificationWorkflowListener` để tự động gửi thông báo (In-App & Email) kèm mã PIN mới cho tất cả sinh viên đang ở trong phòng mỗi khi Admin reset PIN.
* **Hoàn thiện Giao diện Quản lý (Frontend):**
  * **UX/UI & Bảo mật:** Cập nhật `RoomCard.tsx` để hiển thị PIN kèm tính năng "Nút con mắt" (Toggle Visibility) chống nhìn trộm (Shoulder-surfing). Thêm nút chức năng Reset PIN vào Action Menu.
  * **Tách biệt nghiệp vụ (Separation of Concerns):** Xác nhận phân tách giao diện rõ ràng giữa Kỹ thuật viên IoT (quản lý mạch ESP32 ở tab Gate Management) và Quản lý KTX (quản lý mã PIN/Sinh viên ở tab Room Management).
  * Đã Build thành công Frontend.
* **Cập nhật Luận văn & Phân tích Database:**
  * Bổ sung "Quy trình Cấp phát Mã PIN Phòng" và Ma trận phân quyền API vào tài liệu lõi `room_module_thesis.md`.
  * Rà soát 25 bảng CSDL hiện tại và vạch ra chiến lược Tối ưu hóa Database (Giảm xuống còn khoảng 17 bảng) bằng cách: Gộp Single Table Inheritance cho hệ thống Đơn từ, Gộp Billing/Payment, Gộp Vector AI, và chuyển Log IoT sang Redis.

## 2. Hiện Trạng Hệ Thống (Current State)
* **Trạng thái:** Toàn bộ hệ thống Backend, Database (đã migrate V44), Frontend và IoT Firmware đã **đồng bộ 100%** cho quy trình Quản lý Phòng và Kiểm soát Ra vào (Smart Access - FaceID, RFID, PIN). Code đã build và chạy ổn định không lỗi.

## 3. Lý do lưu tài liệu
Tài liệu này được lưu trữ theo LUẬT LƯU LỊCH SỬ (SESSION HISTORY RULE) nhằm mục đích theo dõi vĩnh viễn tiến độ hoàn thành các module của dự án đồ án tốt nghiệp.
