# Lịch sử Phiên làm việc (Session Work Log) - Ngày 08/07/2026

**Người thực hiện:** Master AI Agent
**Trạng thái:** Thành công (Phiên làm việc Buổi sáng + Chiều + Tối)

## Các công việc đã hoàn thành (Phiên Sáng):

1. **Thiết lập Kỷ luật Lưu trữ:**
   - Tạo mới thư mục `docs/work_logs/` và file `README.md` để hướng dẫn chi tiết cách thức ghi log tiến độ.
   - Tạo file log `session_2026_07_07.md` để ghi nhận thành quả từ phiên làm việc của ngày hôm trước.

2. **Cập nhật Bộ Luật Hệ Thống (Governance):**
   - Áp dụng thêm 4 Đạo luật mới vào toàn bộ 5 file `AGENTS.md` của Monorepo: `SESSION HISTORY RULE`, `DOCUMENT PLACEMENT RULE`, `MANDATORY SELF-VERIFICATION RULE`, `NO GARBAGE RULE`, `DETAILED TESTING GUIDANCE RULE`.

3. **Kiểm toán Chéo (Cross-Audit) Hệ thống eKYC - Nhận diện khuôn mặt:**
   - **Admin Web:** Quét trực tiếp file `FaceApprovalQueue.tsx`, xác nhận đã code đầy đủ giao diện.
   - **Mobile App:** Ban hành "Chỉ thị Kiểm toán Tối cao" để bắt buộc dọn sạch rác Kotlin (local AI cũ).

4. **Sửa lỗi Backend (Hibernate Vector Mapping):**
   - Thay thế Derived Query thành Native/JPQL `@Modifying` DELETE trong `FaceEmbeddingRepository` để fix triệt để lỗi `PSQLException`. Đã compile thành công 100%.

5. **Nâng cấp Giao diện Web & Logic mạch ESP32-CAM (IoT):**
   - Viết lại toàn bộ trang `index.html` nhúng trong C++ với UI Dark mode cực đẹp.
   - Sửa logic `StreamServer` để trả về status Real-time qua Fetch API.

6. **Hoạch định Roadmap Tương lai:**
   - Lập tài liệu `06_IOT_HARDWARE_MODEL_EXTENSION.md` thiết kế sa bàn vật lý.

---

## Các công việc đã hoàn thành (Phiên Chiều - Phân hệ Smart Access):

7. **Tối ưu Hóa UI/UX - Web Admin (sdms-frontend):**
   - Thiết kế lại trang `SmartAccessManagement.tsx` chuyên sâu theo chuẩn **Decision Support (Hỗ trợ Quyết định)**.
   - Thêm tính năng **Tra cứu cá nhân (Targeted View)** theo Student ID.
   - Tô màu mã lỗi (Vàng: Giờ giới nghiêm, Đỏ: Sai thẩm quyền) và tích hợp nút **Mở Cổng Hỗ Trợ** trực tiếp vào từng dòng log.
   - Xây dựng **Giao diện Khẩn cấp (Emergency)** cao cấp: Nút bấm có hiệu ứng Đập nhịp tim (Pulse Animation), Checkbox chống click nhầm.
   - Hỗ trợ chọn **Phạm vi khẩn cấp (Scope)**: Có thể chọn Toàn khu hoặc Chỉ áp dụng cho 1 Tòa nhà cụ thể (Đã fix lỗi API `roomApi` trả dữ liệu trực tiếp từ interceptor).

8. **Tài liệu hóa App Sinh viên (Mobile App):**
   - Cập nhật thêm **Mobile App Routing Rule** vào `AGENTS.md` toàn cục.
   - Tạo file `docs/api/student_app_history_flow.md` định nghĩa Luồng gộp 2 API (Verification + Access History) thành một **Unified Timeline** (Kèm thuật toán O(N) và cách chống crash Null Pointer). Sẵn sàng chuyển giao cho team Mobile.

9. **Quy hoạch Kiến trúc IoT (IoT Master Data):**
   - Tạo Roadmap `07_IOT_DOOR_STATE_MACHINE.md` để chính thức hóa 3 trạng thái cửa vật lý: Auto-close (Đóng sau 5s), Lockdown (Tử thủ), và Evacuation (Mở toang).

## Các công việc đã hoàn thành (Phiên Chiều - Phân hệ Auth & Account Management):

10. **Tối ưu Cơ sở dữ liệu và Luồng xử lý lỗi:**
    - Vá lỗi Migration V37 trên PostgreSQL liên quan đến thiếu cột `is_deleted`.
    - Xử lý triệt để lỗi ép kiểu `LOWER(bytea)` của PostgreSQL khi Hibernate truyền giá trị `null` vào JPQL Query. Sử dụng kỹ thuật ngầm định gán chuỗi rỗng và `CAST(:role AS string)` chuẩn JPA/Hibernate 6.
    - Sửa lỗi Mismatch Class `PaginatedResponse` thành `PageResponse` trong toàn hệ thống. Đã verify bằng lệnh `mvn compile` thành công 100%.

11. **Hoàn thiện Tính năng Quản lý Tài khoản (Web Admin):**
    - Hoàn chỉnh trang `AccountManagementPage.tsx` có tính năng tìm kiếm, phân quyền động và bóc tách dữ liệu chuẩn xác qua `axiosClient`.
    - Xây dựng thành công tính năng "Khóa/Mở khóa" (Toggle Lock - Soft Delete) tài khoản để đảm bảo Audit Trail.
    - Xây dựng API và giao diện "Thêm Nhân Viên (Create Staff)" (`POST /api/v1/admin/accounts/staff`).
    - Biên dịch và kiểm tra Frontend với `npm run build` không lỗi TypeScript.

12. **Giao diện Khôi phục Mật khẩu (Forgot Password):**
    - Đã đọc `AGENTS.md` (Frontend) và tạo tài liệu đặc tả `docs/ui/forgot_password_flow.md`.
    - Xây dựng 2 trang: `ForgotPasswordPage.tsx` và `ResetPasswordPage.tsx` theo chuẩn CRD (Không để Admin tự sửa mật khẩu của Staff).
    - Tích hợp Lazy Routing vào `AdminRoutes.tsx` và thêm nút chuyển hướng vào `LoginPage.tsx`.
    - Đã chạy `npm run build` thành công.

13. **Đồng bộ hóa UI/UX - Quản lý Tài khoản (Account Management):**
    - Áp dụng triết lý thiết kế (Design System) từ trang `SmartAccessManagement.tsx` sang `AccountManagementPage.tsx`.
    - Đồng bộ hóa các thành phần `Paper` bo góc (borderRadius: 3), `Box` phân tách Header, Nút bấm hiệu ứng nổi (box-shadow hover) và `CustomSkeleton` khi loading.
    - Cập nhật Data Table phân trang với `TablePagination` nguyên bản thay vì `Pagination`.
    - Căn chỉnh form tạo Staff (Modal) và Notification (Snackbar) cùng một chuẩn `anchorOrigin={{ vertical: 'top', horizontal: 'center' }}`.
    - Xây dựng tính năng "Xem Hồ sơ Sinh viên": Tạo Endpoint `GET /v1/admin/accounts/{id}/student-profile` ở Backend (nằm trong `AdminAccountController` và `UserService`) và Modal Dialog hiển thị thông tin chi tiết bằng `Grid` 2 cột đẹp mắt bên Frontend.

14. **Tài liệu hóa (Single Source of Truth):**
    - Khởi tạo tài liệu đặc tả API chuẩn: `docs/api/admin_account_management_api.md`.

## Lý do đặt file tại đây:
- Đặt tại `docs/work_logs/` theo đúng `SESSION HISTORY RULE` nhằm lưu trữ vĩnh viễn tiến trình dự án.

## Các công việc đã hoàn thành (Phiên Chiều Muộn - Phân hệ Đăng ký Lưu trú & Setup Handoff):

15. **Xử lý Kiến trúc Nghiệp vụ Đăng ký (Registration Workflow):**
    - Nhận diện sự cần thiết của **Mã số sinh viên (studentCode)** đối với việc thiết kế lại luồng Đăng ký nội trú. Không còn phân chia rườm rà giữa "Tân sinh viên" và "Cựu sinh viên".
    - Viết `V38__add_student_code_to_applications.sql` để đưa `student_code` vào CSDL. 
    - Sửa lỗi Migration Crash (`ERROR: column already exists`) do cơ chế auto-ddl của Hibernate can thiệp trước Flyway bằng cách thêm `IF NOT EXISTS`.

16. **Tối ưu Module Quản lý Đợt Đăng Ký (Admin Web):**
    - Bổ sung trường `studentCode` vào file `EligibilityResponse.java` (Backend DTO) và `RegistrationEligibilityService.java`.
    - Điều chỉnh giao diện Frontend (`EligibilityManagerDialog.tsx` và `useEligibilityManager.ts`) để hiển thị cột **Mã sinh viên (Student Code)** trên bảng dữ liệu Excel Whitelist (Data Filtering).

17. **Tài liệu hóa Học thuật cho Luận văn (Academic Documentation):**
    - Phân tích và biên soạn các tài liệu map trực tiếp vào yêu cầu đồ án:
      - `docs/business/thesis_mapping/03_registration_module.md`
      - `docs/business/thesis_mapping/04_registration_period_management_module.md` (Giới thiệu Single Source of Truth, O(1) Set Lookup, Composite Unique Constraint).
    - Tạo `docs/roadmap/features/01_REGISTRATION_REFACTOR_STUDENT_CODE.md` phân tích sâu sự khác biệt giữa Luồng cũ (Dùng CCCD) và Luồng đề xuất (OCR + Student Code).

18. **Thiết lập Giao thức Bàn giao (Handoff Protocol):**
    - Tuân thủ Luật `HANDOVER PROTOCOL`, tạo và cập nhật `docs/handoff/HANDOFF_SUMMARY.md` ghi nhận trạng thái luồng Đăng ký là **PENDING DECISION** (Chờ người dùng đi khảo sát thực tế về mới quyết định). Đảm bảo Agent phiên sau không tự ý code sai hướng.

## Các công việc đã hoàn thành (Phiên Tối - Tối ưu Hóa và Tách biệt Nền tảng Web/Mobile):

19. **Dọn dẹp mã nguồn (Web Admin):**
    - Phân tích các file giao diện Admin và phát hiện 2 file rác không được Router sử dụng (`CheckoutManagementPage.tsx` và `ExtensionManagementPage.tsx`). 
    - Đã xóa vĩnh viễn để làm sạch codebase theo quy tắc `NO GARBAGE RULE`.

20. **Tạo tài liệu tích hợp API cho hệ sinh thái Mobile App:**
    - Viết tài liệu `docs/api/mobile_api_integration_guide.md` tổng hợp API cho App Sinh viên.
    - Viết tài liệu `docs/api/admin_mobile_api_integration_guide.md` tổng hợp API cho App Ban quản lý (Admin).
    - Đảm bảo tuân thủ quy tắc `API Documentation Centralization`.

21. **Quy hoạch Kiến trúc UX/UI (Web vs Mobile):**
    - Đề xuất và chốt định hướng phân tách vai trò:
      - **Web Admin:** Đóng vai trò "Bàn làm việc" tập trung cấu hình vĩ mô, xem biểu đồ, xuất báo cáo (Macro Management).
      - **Mobile App Admin:** Đóng vai trò "Tác nghiệp hiện trường" tập trung vào thao tác nhanh, cơ động như quét QR Check-in, mở khóa cửa khẩn cấp, duyệt đơn nhanh vuốt chạm (Micro Management).
