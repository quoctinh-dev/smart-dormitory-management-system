# WORK LOG - PHIÊN LÀM VIỆC NGÀY 15/07/2026

**Dự án:** Smart Dormitory Management System (SDMS)
**Module tác động:** sdms-frontend, sdms-backend, Business Documentation

## Chi tiết công việc
1. **Sửa lỗi Build Frontend (Hotfix):**
   - Loại bỏ lỗi trùng lặp từ khóa khai báo `isSubmitting` trong file `RegistrationPeriodManager.tsx`.
   - Kết quả: Build thành công (100% Pass).

2. **Nâng cấp tính năng Thanh toán (Backend):**
   - Thay đổi logic tại `PaymentService.java`.
   - Tích hợp chuẩn VietQR động thay vì Link Checkout tổng quát. Dữ liệu QR được ánh xạ thẳng với tài khoản MBBank thật của User (`0819281512`), giúp tăng độ chân thực cho Đồ án.

3. **Thiết lập kết nối Webhook - Internet to Localhost:**
   - User gặp khó khăn vì SePay không thể bắn Webhook về `localhost:8080`.
   - Đã xử lý thông qua việc cài đặt và cấp quyền Tunneling với **Ngrok**. Mạng đã thông luồng.

4. **Tư vấn tích hợp SePay Step-by-Step:**
   - Dẫn dắt User tạo tài khoản SePay Free Tier.
   - Giải quyết tình huống User nhầm lẫn sang hệ thống Bank Hub.
   - Hướng dẫn thiết lập API Token, Secret Key để xác thực và phòng ngừa rủi ro giả mạo giao dịch (Webhook Signature Validation).

## Lý do lưu log:
Lưu vết các quyết định kỹ thuật và trạng thái bàn giao khi User có yêu cầu tạm dừng công việc. Đảm bảo Single Source of Truth (SSOT).
