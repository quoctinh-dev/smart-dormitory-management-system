package com.sdms.backend.modules.room.enums;

/**
 * Phân loại giới tính dành riêng cho Tòa nhà (Building).
 * Khác với Sinh viên chỉ có MALE/FEMALE, Tòa nhà có thể dùng chung (MIXED).
 */
public enum BuildingGender {
    MALE,
    FEMALE,
    MIXED
}
