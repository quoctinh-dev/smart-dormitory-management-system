import { Box, Typography } from '@mui/material';
import React from 'react';

const ExtensionManagementPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Extension Management</Typography>
    </Box>
  );
};

export default ExtensionManagementPage;
