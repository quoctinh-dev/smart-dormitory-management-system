import { Box, Typography } from '@mui/material';
import React from 'react';

const IntegratedReviewPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Integrated Review</Typography>
    </Box>
  );
};

export default IntegratedReviewPage;
