import { Box, Typography } from '@mui/material';
import React from 'react';

const CheckoutManagementPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Checkout Management</Typography>
    </Box>
  );
};

export default CheckoutManagementPage;
